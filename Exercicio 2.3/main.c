#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("informe um valor:");
    scanf("%d", &num);
    //num = num + 1;
    //num += 1; num é o mumero real onde é acrescentado +1
    //num++ ou ++num o num soma + 1 se tornado no mumero real +1
    printf("novo valor: %d\n", num);
    return 0;
}
